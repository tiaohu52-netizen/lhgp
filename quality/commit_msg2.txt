fix(tick) + test(synthesize): wire spec envelope + recursive walk + ack transaction

4 个并行审查子代理（spec 派发 / A2A 投递硬化 / 债务与质量门 / E2E
完整性）的发现都修了：

[synthesize_stage_draft：envelope 不完整，2 个真 bug]
- envelope 只带 goal + acceptance boolean body，把用户写的
  time_budget.deadline_at / budget.max_dispatches / dependencies /
  artifacts / permissions.modifiable_scope 全部丢了
- 只迭代 spec.acceptance["all"]；{"any": [...]} 组合子的机器
  判据被吞，回退到占位 artifact-present:stage:<id>，plan gate 拿到
  的是占位 ID 而不是真实的 file-exists:<target>
- 修：envelope 从 stage 顶层把 scope / dependencies / artifacts /
  time_budget / budget / permissions 全部传进 StageSpec.from_dict
- 修：新增 _collect_machine_checks 递归走 all/any/leaf 判据，
  提取所有 machine leaf 的 kind+target

[A2A 投递：dedup 与 ack 没在同一事务]
- _record_directive_acks 文档承诺"same transaction"，实际 dedup
  UPDATE 和 append_event 各自动 commit（autocommit 模式）
- 用 longtask.persistence.store.transaction 包裹 dedup write + 所有
  ack 事件写入；runner 已在 attempt-finish 事务里调用，嵌套无副作用

[E2E Phase 7 是套套逻辑]
- 旧版只查表行数，没真跑 stage-3 的自动创建路径
- 修：append stage-2 的 verifier attempt/succeeded → run_daemon_tick
  → 断言 stage-2 留 active + acceptance/status-changed 事件 +
  stage-3 不存在

[债务清理]
- 3 处模块 docstring 删到一行：auto_approve.py、stage.py、
  test_stage_spec.py
- acceptance.py spec 字段注释 5 行压成 1 行

[补测试：test_synthesize_spec.py，11 单元]
- all 组合子、any 组合子、嵌套组合子、跳过 user 判据
- deadline 从 time_budget 透传、budget 三个字段透传、
  dependencies/artifacts/modifiable_scope 透传 + hard_constraints
- _has_recent_plan_approval 四个 spec_hash 组合：match / mismatch
  / contract-lost-spec / approval-without-hash + contract-also-without-hash

质量门 7/7 PASS（format / lint / arch / deps / claims / typecheck /
test+coverage 76.13%）。1211 passed, 8 skipped。

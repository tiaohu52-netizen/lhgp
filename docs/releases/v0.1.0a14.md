<!-- 发布说明：本目录存放每个发布版本的 GitHub Release 正文，与标签一一对应。
     发布命令（认证后一条命令即可，正文直接引用本文件）：
     gh release create v0.1.0a14 --title "v0.1.0a14 — 限期合同中枢" \
       --notes-file docs/releases/v0.1.0a14.md --verify-tag \
       dist/longtask_protocol-0.1.0a14-py3-none-any.whl \
       dist/longtask_protocol-0.1.0a14.tar.gz
     本文件是正文的唯一来源：不要在网页上另写一份，否则两处会漂移。 -->

# v0.1.0a14 — 限期合同中枢（Deadline Contract Hub）

本版把 **a13 之后的十一轮增量**与**一轮完整外部审计的整改**合并交付，也是 Releases
页自 `v0.1.0a6` 之后重新跟上源码当前状态的版本。

审计覆盖五个面——CLI 与守护进程、持久化存储、RPC 双命名空间、文档承诺、制品——
逐条按「先实测、再分类、然后修复或去广告」处置：凡修复都先做反向验证（测试先红后
绿），凡结论都以可复现的测量为据，被实测推翻的审计假设逐条记为**不成立**而不是照单
改代码。

## 修复（A 类真缺陷）

- **合同状态机在唯一写入口强制**：`LEGAL_TRANSITIONS` 一直存在，RPC handler 也一直
  在守它（approve/pause/resume/cancel/arbitrate 五处），但 `update_contract_state`
  自己不校验——同一个非法转移「走 RPC 被拒、走守护进程或直调却能落库」，审计记录与
  实际状态可以互相矛盾。现在非法写入抛 `IllegalStateTransitionError`（写库之前，
  `state`/`revision` 都不动），RPC 边界映射为 `STATE_FORBIDDEN`
  （`RETRYABLE=False`）。**自反写入（X→X）放行**：tick 的
  `_judge_verifier_outcomes` 在验收失败时写 `new_state=ACTIVE` 而合同本就是 active，
  它要做的是把 `acceptance_status` 置 FAILED。实测依据：全量测试埋点记录了 src 侧
  13 种真实转移（全部合法或自反），而 15 次非法转移全部来自测试绕过 handler 走捷径，
  已逐个改为合法路径。
- **损坏行在读路径上自解释地失败**：`contract/get`、`goal/prepare`、
  `goal/admission-check` 三个 handler 完全没有 store 异常映射，损坏行会以
  `json.JSONDecodeError`/`KeyError` 这类不属于 `StoreError` 层级的裸异常穿过 RPC
  边界；且映射靠每个 handler 手写（本包内三种写法），漏写即漏报。改为类型化
  （`StoreTamperedError`）+ 边界统一映射（`STORE_TAMPERED`）。
- **`patch_contract` 的修订快照不再丢字段**：手写枚举 15 个字段漏了带
  `default_factory` 的 `auto_approve`——不报错、不为空，而是静默填入安全基线，于是
  每个被 patch 的修订都把「已授予自动批准」记成「未授予」，而活行仍是真实授权
  （审计记录里一次静默的授权降级）。改用 `dataclasses.replace`。
- **attempt 生命周期事件必须带真实角色**：`_fail_attempt`/`_mark_stale`/取消路径与
  SPEC §12.4 **通道 1**（会话型 harness 的 `attempt/write-back`）写入的事件 `role`
  列曾是 `None`/`'model'`，而值域是 executor/verifier/daemon/user/promoter/
  scheduler/system。后果：核验器成功的事件在 tick 裁决里**一条都看不到**（实测修复
  前 0 条、修复后 1 条），合同因此无法完成。
- **执行器并发额度不再被终态 attempt 永久占用**：容量记账手写
  `('admitted','running','orphaned')`——把终态 `orphaned` 算成在跑（一条失联 attempt
  永久占死额度），又漏掉 `starting`/`waiting`（额度被超额放行）。改为从
  `AttemptState` 推导在飞集合。
- **消除 CAPACITY_FULL 热转**：容量饱和时把 `next_decision_at` 置 `now`，与「blocked
  的过期决策点原样返回」相乘 → 守护进程每轮算出 `until=0` → 不睡 → CPU 空转。
- 另有：`request_id` 跨合同归属守卫补齐到 canonical 侧、分发表漂移与 canonical 门面
  缺导出、wheel 漏 `flowgen` skill、operator profile 重复/过期工具条目、拒接的验收
  请求不再被误标为已兑现、stdout 协议通道与子进程编码入规范并钉住。

## 新增

- `evidence` 验收证据独立表（SPEC §13.1，schema v6）、attempt 消耗台账（usage 自报 +
  schema v5 + `stats` 聚合）、成本预算线 `budget.max_cost`、MCP 工具面 profile
  （按角色收窄 + 越界拒调）、可复制的无模型本地示例
  （`examples/local-no-model/`：doctor→…→satisfied 全链路）、ADR-005。
- **回归测试净增 90 条**（`1554 → 1644 passed`）：状态机 9×9 全枚举乘积上断言
  「守卫判定 == 表 + 自反规则」而不是手写字面清单；损坏行 9 种形态；修订快照与活合同
  逐字段一致（覆盖 `dataclasses.fields` 全部字段）；`events.role` 值域与两条判定通道；
  门面覆盖与真身方向；行尾策略。

## 文档与诚实性

- **两处未交付能力去广告**（原文字保留 + 追加状态，不静默删除）：
  - **E2 饥饿保护未接线**：`fairness_states` 恒为空、`observe_tick` 只在测试里被调用，
    「连续 5 tick 未派工自动提前」在生产里从不触发；DESIGN §8.3 承诺的是另一套语义
    （额度不足 + u ≥ 1.0 → `blocked(need-user)`，默认 10 轮）。a6 的 CHANGELOG 曾宣称
    已交付，就地更正。
  - **7 个只有名字的 RPC 方法**：`context/refresh`、`context/promote`、
    `control/notify`、`control/followup`、`control/steer`、`control/spawn`、
    `lease/release` 在枚举里有名字但两侧分发表都没有 handler（调用返回
    `STATE_FORBIDDEN: method not implemented`），DESIGN/SPEC 逐处标注「预留未实现」。
- 修正三份方向性文档（ARCHITECTURE 真身地图的 `rpc/handlers` 方向按实测重写、
  canonical 命名空间与入口 shim 方向、CONTRIBUTING 对齐），更正安全加固证据里已被
  实测推翻的判断。
- 新增 `.gitattributes` 声明行尾策略（仓库内存 LF、任何平台检出 LF）。

## 安装

```
python -m pip install longtask_protocol-0.1.0a14-py3-none-any.whl
lhgp --version        # lhgp 0.1.0a14 (protocol v1)
lhgp doctor           # 5 项检查，ALL SYSTEMS GO
```

从源码：`uv sync --extra dev && uv run lhgp --version`。模型侧入口 `lhgp-mcp`
（MCP stdio，39 工具）。

## 支持环境

Windows 10/11（全面验证）· Linux（CI 阻断绿）· macOS（CI 阻断绿）· Python 3.11–3.13。
**运行时零第三方依赖**（全新 venv 实测只装进 `longtask-protocol` 一个包）。

## 候选验证（RELEASE-PLAN §4 R5）

- 七道门 7/7 PASS：`1644 passed, 11 skipped`，覆盖率 79.94%（下限 78%）。
- 制品 companion resources OK（从仓库树推导清单，不手写）。
- 全新 venv 安装后 `lhgp --version` / `doctor` 全过；真链路
  `prepare(drafted, rev1) → list → approve(rev2) → get(state=active)`。
- 证据文档：`quality/evidence/gate-run-20260911-a14.md`。

## 制品校验

本版制品由 `.github/workflows/release.yml` 从**被冻结的标签提交**重建并上传，
SHA-256 由该工作流在上传时计算，附在正文末尾的「制品校验（自动计算）」小节。

此处刻意**不写死哈希**：本地构建与 CI 构建的字节不同（zip/gzip 的时间戳），
写死的数字与真正下载到的文件对不上，等于用精确的格式说一句不精确的话。
本机候选验证时算出的那组哈希记录在 `quality/evidence/gate-run-20260911-a14.md`，
它描述的是本地候选制品，不是这里的附件。

## 已知未交付项

E2 饥饿保护（待独立 SPEC 与测试）、7 个预留 RPC 方法、跨关机唤醒的 L2/L3 云侧能力、
提案的档 4 并行加派——均在 `quality/claims.json` 中以 `accepted_debt` 登记，不假装
已交付。本版无破坏性变更：`PROTOCOL_VERSION` 仍为 1。
